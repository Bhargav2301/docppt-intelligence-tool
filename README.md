# 📄 DocPPT Intelligence Tool

> **Transform AI-generated or AI-assisted PowerPoint decks into natural, presentation-ready language — with explainable AI-likeness detection and layout-safe humanization.**

---

## 🔄 Scope Change Notice

> **v2 Update:** The original Google Docs document-summarizer, spec-generator, and requirements-extractor pipeline has been **removed entirely** from this project. The tool is now a focused **PPTX AI-Likeness Detector and Humanizer**. All routes, UI screens, and backend modules related to document parsing (`.docx`, Google Doc URLs), local summarization (LexRank, TextRank, DistilBART, Flan-T5), spec generation, and requirements extraction have been deprecated and cleaned from the codebase.

---

## 🚀 Overview

The **DocPPT Intelligence Tool** is a web-based PPTX intelligence platform that solves a common pain point for consultants, product managers, founders, and freelance builders: receiving AI-assisted slide decks that read like they were generated by a language model rather than written by a human presenter.

The tool accepts a `.pptx` upload, runs an explainable AI-likeness analysis on every text element across all slides, and rewrites flagged text into natural, concise, audience-ready language — while preserving the deck's structure, layout, non-text assets, and formatting with 100% fidelity.

All analysis and rewriting operates through **free, local, and small-model NLP pipelines** using deterministic feature engineering and optional local language model scoring rather than relying on paid external LLM APIs.

---

## 🛠️ Key Features

### 1. PPTX Text Extraction

- Extracts text slide-by-slide and shape-by-shape, keeping track of `slide_id`, `shape_id`, `paragraph_id`, and `run_id` for layout-safe round-trip reconstruction.
- Identifies the semantic role of each text block: title, subtitle, bullet, body, caption, table cell, or speaker note.
- Stores raw and normalized copies separately so analysis never corrupts source data.

### 2. Artifact Detection

Deterministic, rule-based detection of visible cleanup issues before style analysis:
- **Citation residue:** `[1]`, `[Source]`, `(Source)`, superscript-style references rendered as plain text.
- **Markdown leakage:** `**bold**`, `# heading`, backticks, raw list markers copied from markdown editors.
- **Smart-character and delimiter noise:** stray pipes, em-dash misuse, duplicated punctuation, malformed quotes.
- **Generation framing leftovers:** "Certainly,", "Here's a refined version:", "In conclusion,", "Overall,", "It is worth noting that".

Every auto-applied cleanup is logged for user review.

### 3. AI-Likeness Analyzer

Computes an explainable AI-likeness score for each text element using measurable linguistic signals across seven feature groups:

| Feature Group | Key Signals |
|---|---|
| Surface features | Sentence length variance, type-token ratio, punctuation density |
| Burstiness and rhythm | Sentence opener repetition, uniform bullet parallelism |
| Repetition signals | Tri-gram repeats, generic business phrase density |
| Lexical specificity | Abstract vs. concrete noun ratio, vague adjective share |
| Syntactic patterns | Passive voice rate, modifier stacking, compound structure overuse |
| Readability | Flesch-Kincaid grade, Gunning fog, discourse transition frequency |
| Optional model signals | Local language model perplexity, embedding anomaly distance |

Scores map to three bands: **low**, **moderate**, and **high** AI-likeness. Each result includes specific, human-readable reasons rather than a single opaque number.

The tool never claims authorship certainty. It identifies **AI-like phrasing patterns** only.

### 4. Humanization Rewriter

A multi-stage controlled rewrite pipeline that diagnoses each text element's specific weaknesses and applies targeted corrections:

- **Cleanup transforms:** remove artifacts, normalize punctuation, strip generation framing.
- **Compression transforms:** delete redundant modifiers, remove restatements, convert abstract noun phrases to direct operational verbs.
- **Specificity transforms:** replace generic business nouns with concrete domain language already implied by context.
- **Rhythm transforms:** vary sentence openings across adjacent bullets, mix clause lengths, avoid uniform grammatical form.
- **Tone transforms:** match one of five user-selectable tone presets — `presentation_concise`, `executive_polished`, `founder_clear`, `product_manager_direct`, `consulting_professional`.

Multiple rewrite candidates are generated and ranked by a Judge agent that prioritizes meaning preservation and slide fit over detector score reduction.

### 5. Layout-Safe PPTX Export

- Recompiles the deck with modified text runs at the exact source location (`slide_id`, `shape_id`, `paragraph_id`, `run_id`).
- Validates character count delta, line-break risk, and text-box overflow before inserting each rewrite.
- Classifies each change as `safe_replace`, `needs_shorter_option`, or `manual_review`.
- Guarantees 100% fidelity for all non-text assets: images, shapes, tables, charts, animations, and slide count.

### 6. Human-in-the-Loop Review UI

- Per-slide AI-likeness badges with expandable "Why this was flagged" panels.
- Side-by-side before/after diff view with inline change highlighting.
- Rewrite intensity selector: `Minimal`, `Balanced`, or `Strong`.
- One-click revert to original text per slide.
- Safety labels surface `manual_review` items before export.
- Tone preset and local model settings persist via `/settings`.

---

## 📂 Repository Structure

```
docppt-intelligence-tool/
├── apps/
│   └── web/                   # Next.js frontend (React 19, TypeScript, Tailwind CSS v4)
├── services/
│   └── nlp/                   # FastAPI backend service (Python)
│       └── app/
│           ├── detector/      # AI-likeness feature engineering and scoring
│           ├── humanizer/     # Rewrite planning, generation, and judging
│           └── ppt/           # PPTX extraction, layout checks, and export
├── docs/                      # Architecture, PRD, TRD, and deployment guides
├── render.yaml                # Render deployment Blueprint
└── README.md
```

---

## ⚡ Getting Started

### 📋 Prerequisites

- **Frontend:** Node.js v20+ and `npm`
- **Backend:** Python 3.10+ and `pip`
- **Database:** SQLite (local development default) or PostgreSQL (production)

---

### 🔧 1. Backend Setup (`services/nlp`)

```bash
cd services/nlp

# Create and activate virtual environment
python3 -m venv venv
source venv/bin/activate        # Windows: venv\Scripts\activate

# Install dependencies
pip install -r requirements.txt

# Start development server
uvicorn main:app --reload --port 8000
```

FastAPI docs: [http://localhost:8000/docs](http://localhost:8000/docs)

> **Note:** On first startup the server runs database migrations and seeds a default developer account:
> - **Username:** `local_user@example.com`
> - **Password:** `password`

---

### 💻 2. Frontend Setup (`apps/web`)

```bash
cd apps/web
npm install

# Create environment config
echo "NEXT_PUBLIC_API_BASE_URL=http://localhost:8000" > .env.local

# Start development server
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) in your browser.

---

## 🔌 API Reference

| Router / Path | Method | Purpose |
|---|---|---|
| `/auth` | POST | Login, registration, JWT issuing |
| `/sessions` | GET / POST | Save and recall user processing sessions |
| `/models` | GET | Status of local and loaded NLP pipelines |
| `/ppt` | POST | Upload and parse `.pptx` files |
| `/ppt_analysis` | POST | Detect artifacts and AI-likeness per slide element |
| `/rewrite` | POST | Generate humanized rewrites for flagged text |
| `/eval` | POST | Score rewrite quality against source text |
| `/settings` | GET / POST | Tone presets, local model endpoints, user preferences |
| `/export` | POST | Download the rewritten PPTX (layout-safe) |
| `/telemetry` | POST | Pipeline performance and quality logs |
| `/health` | GET | API heartbeat |

> **Removed routes:** `/doc` (document parsing) and `/analysis` (spec generation) are no longer available. Requests to these routes will return a `410 Gone` response with a deprecation message.

---

## 🌐 Production Deployment

The project is configured for single-click deployment to **Render** using the `render.yaml` Blueprint.

### Services spawned

1. **`docppt-backend`** — Docker-based FastAPI container.
2. **`docppt-frontend`** — Docker-based Next.js build.
3. **`docppt-db`** — Managed PostgreSQL (free tier).

Render automatically injects `envVars` as Docker build arguments, ensuring `NEXT_PUBLIC_API_BASE_URL` is baked into the Next.js static asset build phase. To deploy, link this repository to Render under the Blueprints page.

---

## 🧪 Testing

Run backend tests:

```bash
cd services/nlp
pytest tests/ -v
```

Key test modules:

| Test file | Coverage |
|---|---|
| `test_features.py` | Feature computation functions |
| `test_rules.py` | Artifact detection rules |
| `test_scorer.py` | Score band boundary conditions |
| `test_planner.py` | Rewrite strategy selection |
| `test_rewriter.py` | Candidate generation per tone preset |
| `test_judge.py` | Candidate ranking and rejection |
| `test_layout_checks.py` | Text-box overflow detection |

Integration tests cover the full round-trip: upload → analysis → rewrite → export → PPTX reconstruction verification.

---

## ⚠️ Known Constraints

- Input must be `.pptx` format only. `.docx`, Google Doc URLs, and raw text input are not supported.
- The AI-likeness score is a style-pattern signal, not an authorship claim. Short text elements (under 20 words) receive score dampening.
- Rewrites that exceed the estimated character limit for a text box are flagged as `needs_shorter_option` and routed back for a tighter candidate. Auto-apply is blocked until a safe rewrite is confirmed.
- Speaker notes are analyzed and rewritten separately from slide body text and are never inserted into slide shape runs.

---

## 📄 License

MIT
